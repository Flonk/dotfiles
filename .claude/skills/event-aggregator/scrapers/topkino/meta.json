{
  "slug": "topkino",
  "name": "Top Kino",
  "url": "https://www.topkino.at/",
  "tier": "B",
  "group": "cinema",
  "covers": [
    "cinema"
  ],
  "status": "built",
  "method": "html",
  "expect": {
    "min_records": 47,
    "required_fill": {
      "description": 1.0,
      "venue": 1.0,
      "district": 1.0,
      "image": 0.9
    },
    "canary": "data-termin_id"
  },
  "last_built": "2026-08-08",
  "notes": "Single-venue cinema; gathers film detail URLs from /kino/aktuell + /kino/demnaechst, then reads each /kino/<slug> (or veranstaltung_detail?veranstaltung_id=) page's Termine list; real showtimes only published ~2 weeks ahead, plus occasional far-out premiere placeholder dates (date-only, no room/price); no genre/category or per-showtime price on site (general 8,50 EUR base price mentioned once in a sidebar, not attributable per record, so price_min left null); image: the FIRST <img> on the page is the site logo (/resources/images/logo-weiss.png) so it is deliberately NOT used — instead the main still is the <img> immediately preceding the <div class=\"kurzbeschreibung\"> block (100% fill observed, anchored to the same landmark KURZ_RE already uses); runtime/origin text ('Regisseur, Land, Jahr NN min., Version') stored verbatim as extra.origin_year_runtime (100% fill) for the pipeline's automatic end derivation; no direct end time published so end stays null",
  "schedule": {
    "cadence": "weekly",
    "horizon_days": 14,
    "basis": "measured",
    "density_per_day": 1.447,
    "p95_lead_days": 12,
    "sample_records": 68,
    "sample_truncated": false
  }
}
