#! /bin/bash

cat << EOF

┏━┓┏━┓╺┳╸
┣━┫┣━┛ ┃
╹ ╹╹   ╹
EOF

sudo apt install -y zsh rofi feh neofetch polybar # general rice
sudo apt install -y maim xbacklight playerctl pulseaudio wmctrl # for my i3+polybar setup
sudo apt install -y picom # compositor

sudo apt install -y xclip xsel xdotool # clipboard management
sudo apt install -y xcape # so I can open a menu when I tap the windows key

sudo apt install -y cmake # build tooling

sudo apt install -y blueman # semi-cool ui for managing bluetooth devices. ("Bluetooth Manager")
                            # better than having to use bluetoothctl but TODO find something better

sudo apt install -y net-tools zip qalc # terminal utilities

sudo apt install -y ffmpeg vlc # video
sudo apt install -y wine # just in case
 