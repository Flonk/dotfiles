#! /bin/bash

cat << EOF

┏━┓┏━┓╺┳╸
┣━┫┣━┛ ┃
╹ ╹╹   ╹
EOF

sudo apt install -y nala # apt but better

sudo nala install -y zsh rofi feh neofetch polybar # general rice
sudo nala install -y maim xbacklight playerctl pulseaudio wmctrl # for my i3+polybar setup
sudo nala install -y picom # compositor

sudo nala install -y xclip xsel xdotool # clipboard management

sudo nala install -y cmake # build tooling

sudo nala install -y blueman # semi-cool ui for managing bluetooth devices. ("Bluetooth Manager")
                            # better than having to use bluetoothctl but TODO find something better

sudo nala install -y net-tools zip qalc # terminal utilities

sudo nala install -y ffmpeg vlc # video
sudo nala install -y wine # just in case
sudo nala install -y expect
